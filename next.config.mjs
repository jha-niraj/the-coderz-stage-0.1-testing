/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'avatars.githubusercontent.com', 'picsum.photos', 'lh3.googleusercontent.com', 'tse4.mm.bing.net', 'source.unsplash.com' , 'assets.aceternity.com'],
    },
};

export default nextConfig;
